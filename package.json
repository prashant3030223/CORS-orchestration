{
  "name": "corsguard-monorepo",
  "version": "1.0.0",
  "description": "Enterprise-grade CORS Orchestration & Security Suite",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../user-data-api && npm install",
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && npm run dev\" \"cd user-data-api && npm run dev\"",
    "build": "cd frontend && npm run build",
    "start-backend": "cd backend && npm start",
    "start-api": "cd user-data-api && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "mongodb": "^7.0.0"
  }
}
